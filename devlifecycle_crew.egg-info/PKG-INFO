Metadata-Version: 2.4
Name: devlifecycle-crew
Version: 0.1.0
Summary: AI-powered agent system for managing software development lifecycle using CrewAI
Author-email: DevLifecycle Team <team@example.com>
License: MIT
Project-URL: Homepage, https://github.com/example/devlifecycle-crew
Project-URL: Documentation, https://github.com/example/devlifecycle-crew#readme
Project-URL: Repository, https://github.com/example/devlifecycle-crew
Project-URL: Issues, https://github.com/example/devlifecycle-crew/issues
Keywords: crewai,agents,ai,sdlc,project-management,requirements,tickets,automation
Classifier: Development Status :: 3 - Alpha
Classifier: Intended Audience :: Developers
Classifier: License :: OSI Approved :: MIT License
Classifier: Operating System :: OS Independent
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: Programming Language :: Python :: 3.12
Classifier: Topic :: Software Development :: Libraries :: Python Modules
Classifier: Topic :: Software Development :: Quality Assurance
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: crewai>=0.86.0
Requires-Dist: crewai-tools>=0.17.0
Requires-Dist: openai>=1.57.0
Requires-Dist: langchain>=0.3.0
Requires-Dist: langchain-openai>=0.2.0
Requires-Dist: pydantic>=2.10.0
Requires-Dist: pydantic-settings>=2.0.0
Requires-Dist: python-dotenv>=1.0.0
Requires-Dist: rich>=13.9.0
Requires-Dist: click>=8.1.0
Requires-Dist: pyyaml>=6.0.0
Requires-Dist: jinja2>=3.1.0
Requires-Dist: requests>=2.32.0
Requires-Dist: httpx>=0.28.0
Provides-Extra: dev
Requires-Dist: pytest>=8.0.0; extra == "dev"
Requires-Dist: pytest-asyncio>=0.24.0; extra == "dev"
Requires-Dist: pytest-cov>=5.0.0; extra == "dev"
Requires-Dist: black>=24.0.0; extra == "dev"
Requires-Dist: isort>=5.13.0; extra == "dev"
Requires-Dist: mypy>=1.8.0; extra == "dev"
Requires-Dist: ruff>=0.7.0; extra == "dev"
Provides-Extra: database
Requires-Dist: sqlalchemy>=2.0.0; extra == "database"
Requires-Dist: aiosqlite>=0.20.0; extra == "database"

# DevLifecycle Crew

🤖 **AI-powered agent system for managing software development lifecycle using CrewAI**

DevLifecycle Crew is a comprehensive multi-agent system designed to help development teams manage intake tickets, requirements, and the entire software development lifecycle. Built on [CrewAI](https://github.com/joaomdmoura/crewAI), it leverages specialized AI agents that collaborate to automate and streamline common SDLC tasks.

## 🌟 Features

### Agent Types

| Agent | Role | Capabilities |
|-------|------|--------------|
| **Intake Agent** | Ticket Intake Specialist | Triage, classify, prioritize, and route incoming tickets |
| **Requirements Agent** | Requirements Analyst | Analyze requirements, create user stories, validate completeness |
| **SDLC Manager Agent** | Project Manager | Track progress, manage phases, generate reports, handle blockers |
| **Quality Agent** | QA Specialist | Review testability, create test strategies, assess quality risks |
| **Planning Agent** | Sprint/Release Planner | Estimate work, plan sprints, optimize backlogs |

### Key Workflows

- **Ticket Processing**: Automatic triage, classification, and routing of incoming tickets
- **Requirements Analysis**: Transform raw requests into structured, validated requirements
- **User Story Creation**: Generate INVEST-compliant user stories with acceptance criteria
- **Sprint Planning**: Optimize sprint composition based on capacity and priorities
- **Release Planning**: Create comprehensive release plans with milestones
- **Status Reporting**: Generate detailed project status reports
- **Change Impact Analysis**: Assess impact of proposed changes
- **Quality Gate Checks**: Verify deliverables meet quality criteria

## 📦 Installation

### Prerequisites

- Python 3.10 or higher
- An OpenAI API key (or other supported LLM provider)

### Install from Source

```bash
# Clone the repository
git clone https://github.com/example/devlifecycle-crew.git
cd devlifecycle-crew

# Create a virtual environment
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate

# Install dependencies
pip install -r requirements.txt

# Or install as a package
pip install -e .
```

### Configuration

1. Copy the example environment file:

```bash
cp .env.example .env
```

2. Edit `.env` and add your API keys:

```env
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4o
VERBOSE=True
```

## 🚀 Quick Start

### Using the CLI

The DevLifecycle Crew provides a command-line interface for common operations:

```bash
# Triage a ticket
devlifecycle triage examples/sample_ticket.txt

# Analyze requirements
devlifecycle analyze examples/sample_requirements.txt

# Plan a sprint
devlifecycle plan-sprint examples/sample_backlog.txt \
    --capacity "40 story points" \
    --goal "Complete authentication redesign"

# Generate status report
devlifecycle status examples/sample_project_status.txt

# Start interactive mode
devlifecycle interactive
```

### Using Python API

```python
from devlifecycle_crew import DevLifecycleCrew

# Initialize the crew
crew = DevLifecycleCrew(verbose=True)

# Process a ticket
ticket = """
Title: Login fails on mobile
Description: Users report login page shows blank screen on mobile devices.
Priority: High
"""
result = crew.process_ticket(ticket)
print(result)

# Analyze requirements
requirements = """
Feature: User Dashboard
- Display recent activity
- Show statistics
- Support dark mode
"""
result = crew.analyze_requirements(
    requirements,
    create_stories=True,
    validate=True
)
print(result)

# Plan a sprint
backlog = """
US-001: Authentication (8 pts)
US-002: Dashboard (5 pts)
BUG-001: Fix login (3 pts)
"""
result = crew.plan_sprint(
    backlog=backlog,
    team_capacity="35 story points, 5 developers",
    sprint_goal="Complete authentication module"
)
print(result)
```

### Running the Example

```bash
python -m devlifecycle_crew.main
```

## 📖 Detailed Usage

### Ticket Processing

The intake workflow handles incoming tickets through multiple stages:

```python
from devlifecycle_crew import DevLifecycleCrew

crew = DevLifecycleCrew()

# Single ticket processing
result = crew.process_ticket("""
    Title: API rate limiting not working
    Description: Rate limits are not being enforced for authenticated users.
    Steps: 1. Authenticate 2. Make >100 requests/min 3. No rate limit error
    Expected: Should receive 429 after limit
    Actual: All requests succeed
    Severity: High
""")

# The result includes:
# - Ticket classification (type, priority, severity)
# - Affected components
# - Recommended labels
# - Initial requirements extraction
# - Suggested next steps
```

### Requirements Analysis

Transform raw requirements into structured, validated documentation:

```python
# Full requirements analysis with user stories
result = crew.analyze_requirements(
    requirements_text="""
    We need to add multi-factor authentication to improve security.
    Users should be able to use SMS or authenticator apps.
    Must comply with SOC2 requirements.
    """,
    create_stories=True,  # Generate user stories
    validate=True         # Run validation checks
)

# Returns:
# - Structured requirements (functional & non-functional)
# - User stories with acceptance criteria
# - Story point estimates
# - Validation report with quality scores
# - Testability assessment
```

### Sprint Planning

Create optimized sprint plans:

```python
result = crew.plan_sprint(
    backlog="""
    US-100: User login (8 pts) - Authentication
    US-101: Password reset (5 pts) - Authentication  
    US-102: Dashboard widgets (8 pts) - UI
    US-103: API endpoints (5 pts) - Backend
    BUG-50: Fix session timeout (3 pts)
    TECH-20: Update dependencies (2 pts)
    """,
    team_capacity="""
    Team size: 4 developers
    Sprint duration: 2 weeks
    Capacity: 32 story points
    Notes: One developer on vacation day 1-3
    """,
    sprint_goal="Complete user authentication module"
)
```

### Full Workflow: Tickets to Sprint

Execute a complete workflow from raw tickets to sprint plan:

```python
tickets = [
    "Bug: Login page crashes on Firefox",
    "Feature: Add dark mode support",
    "Enhancement: Improve search performance",
]

result = crew.full_ticket_to_sprint_workflow(
    tickets=tickets,
    team_capacity="40 story points",
    sprint_goal="Address critical bugs and start dark mode"
)

# This workflow:
# 1. Triages all tickets
# 2. Extracts and analyzes requirements
# 3. Creates user stories
# 4. Validates requirements
# 5. Reviews testability
# 6. Creates sprint plan
```

### Custom Workflows

Create custom workflows with specific agent combinations:

```python
workflow_config = {
    "agents": ["intake", "requirements", "quality"],
    "process": "sequential",
    "tasks": [
        {
            "type": "triage",
            "agent": "intake",
            "data": "Bug: Application crashes on startup"
        },
        {
            "type": "analyze_requirements",
            "agent": "requirements",
            "data": "Need to fix startup crash and improve stability"
        }
    ]
}

result = crew.run_custom_workflow(workflow_config)
```

## 🛠️ Available Tools

### Ticket Tools
- `TicketCreatorTool` - Create new tickets
- `TicketSearchTool` - Search existing tickets
- `TicketUpdaterTool` - Update ticket fields

### Requirements Tools
- `RequirementsParserTool` - Parse requirements from text
- `RequirementsValidatorTool` - Validate requirement quality
- `TraceabilityMatrixTool` - Track requirement traceability

### SDLC Tools
- `SprintPlannerTool` - Assist with sprint planning
- `StatusTrackerTool` - Track project status
- `RiskAssessmentTool` - Assess project risks

## 🏗️ Architecture

```
devlifecycle_crew/
├── __init__.py           # Package initialization
├── crew.py               # Main orchestration (DevLifecycleCrew)
├── cli.py                # Command-line interface
├── main.py               # Entry point with examples
├── agents/               # Agent definitions
│   ├── intake_agent.py
│   ├── requirements_agent.py
│   ├── sdlc_manager_agent.py
│   ├── quality_agent.py
│   └── planning_agent.py
├── tasks/                # Task definitions
│   ├── intake_tasks.py
│   ├── requirements_tasks.py
│   ├── sdlc_tasks.py
│   ├── quality_tasks.py
│   └── planning_tasks.py
├── tools/                # Custom tools
│   ├── ticket_tools.py
│   ├── requirements_tools.py
│   └── sdlc_tools.py
└── config/               # Configuration
    └── settings.py
```

## ⚙️ Configuration Options

| Environment Variable | Description | Default |
|---------------------|-------------|---------|
| `OPENAI_API_KEY` | OpenAI API key | Required |
| `OPENAI_MODEL` | Model to use | `gpt-4o` |
| `TEMPERATURE` | LLM temperature | `0.7` |
| `VERBOSE` | Enable verbose output | `True` |
| `MAX_RPM` | Max requests per minute | `10` |
| `DATABASE_URL` | Database connection | `sqlite:///devlifecycle.db` |
| `LOG_LEVEL` | Logging level | `INFO` |

### External Integrations (Optional)

| Variable | Description |
|----------|-------------|
| `JIRA_URL` | Jira instance URL |
| `JIRA_API_TOKEN` | Jira API token |
| `JIRA_EMAIL` | Jira account email |
| `GITHUB_TOKEN` | GitHub personal access token |
| `GITHUB_REPO` | GitHub repository (owner/repo) |

## 🧪 Testing

```bash
# Install test dependencies
pip install -e ".[dev]"

# Run tests
pytest

# Run with coverage
pytest --cov=devlifecycle_crew

# Run specific test file
pytest tests/test_tools.py
```

## 📁 Example Files

The `examples/` directory contains sample input files:

- `sample_ticket.txt` - Example support ticket
- `sample_requirements.txt` - Example requirements document
- `sample_backlog.txt` - Example sprint backlog
- `sample_project_status.txt` - Example project status data

Use these with the CLI:

```bash
devlifecycle triage examples/sample_ticket.txt
devlifecycle analyze examples/sample_requirements.txt
```

## 🔧 Extending the System

### Adding a New Agent

1. Create a new agent file in `devlifecycle_crew/agents/`:

```python
from crewai import Agent

class MyNewAgent:
    @staticmethod
    def create(tools=None):
        return Agent(
            role="Your Role",
            goal="Agent's goal",
            backstory="Agent's backstory",
            tools=tools or [],
        )
```

2. Add tasks in `devlifecycle_crew/tasks/`
3. Register in `crew.py`

### Adding a New Tool

1. Create a tool in `devlifecycle_crew/tools/`:

```python
from crewai.tools import BaseTool
from pydantic import BaseModel, Field

class MyToolInput(BaseModel):
    param: str = Field(description="Parameter description")

class MyTool(BaseTool):
    name: str = "my_tool"
    description: str = "What the tool does"
    args_schema = MyToolInput
    
    def _run(self, param: str) -> str:
        # Tool implementation
        return "result"
```

2. Export in `tools/__init__.py`
3. Assign to relevant agents in `crew.py`

## 🤝 Contributing

1. Fork the repository
2. Create a feature branch (`git checkout -b feature/amazing-feature`)
3. Commit your changes (`git commit -m 'Add amazing feature'`)
4. Push to the branch (`git push origin feature/amazing-feature`)
5. Open a Pull Request

## 📄 License

This project is licensed under the MIT License - see the [LICENSE](LICENSE) file for details.

## 🙏 Acknowledgments

- [CrewAI](https://github.com/joaomdmoura/crewAI) - The multi-agent framework powering this system
- [LangChain](https://github.com/langchain-ai/langchain) - LLM orchestration
- [OpenAI](https://openai.com) - Language models

## 📞 Support

- Create an issue for bug reports or feature requests
- Check existing issues before creating new ones
- Provide detailed reproduction steps for bugs
